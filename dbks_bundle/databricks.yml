# This is a Databricks asset bundle definition for dbks_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbks_bundle
  uuid: 4362d5db-63e8-4363-b68c-a4beb6023265

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-b45abf77-62b1.cloud.databricks.com
    variables:
      catalog: workspace
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-b45abf77-62b1.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/syedshahul2002@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/syedshahul2002@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: syedshahul2002@gmail.com
        level: CAN_MANAGE
